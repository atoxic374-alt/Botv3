# سجل تدقيق العمليات — Botv3

## النطاق والنتيجة

راجع هذا التدقيق مسارات إنشاء البوتات، الحسابات، الجلسات، rate limit، مكتبة التطبيقات، التيمات، Intents، الهوية، الدعوات، Reset Token، وReset All. القاعدة التشغيلية المعتمدة هي أن النجاح لا يُسجّل إلا بعد تأكيد النتيجة من Discord أو من طبقة التخزين المحلية؛ أما النتيجة الجزئية فتظهر كـ `partial` أو `غير مكتمل`، والفشل يحتفظ بمرحلة التنفيذ والسبب المختصر.

## Rate limit والحسابات

يعتمد الكود على `Retry-After` و`retry_after` و`X-RateLimit-Reset-After` و`X-RateLimit-Bucket` و`X-RateLimit-Scope` و`X-RateLimit-Global` بدلاً من حدود ثابتة. يتم التفريق بين route وbucket وglobal، ولا يُستخدم تبديل الحساب كوسيلة لتجاوز global rate limit. الحسابات التي تتوقف مؤقتاً تحتفظ بوقت الاستئناف وتصنيف مثل `rate_limited` و`invalid_token` و`portal_failed` و`cloudflare_block`، وتظهر هذه البيانات في لقطة الجلسة والواجهة.

استمرار الجلسة يتطلب فحص `health.ready` وتجهيز Developer Portal قبل اعتماد الحساب أو إعادة المحاولة. التبديل يتم بعد فحص الحساب والـ Portal، و`tsSleep` يستجيب للإيقاف والإيقاف المؤقت. إذا فشل تحديث Portal بعد الانتظار أو بعد دفعة طويلة فلا تتم إعادة المحاولة وكأن الجلسة جاهزة.

## المكتبة والأزرار

عمليات PFP All وIntents All وBulk Invite وReset All لا تبدأ على مكتبة ناقصة؛ يعاد فحص تطبيقات كل تيم عبر endpoint التيم، ويُوقف التنفيذ عند تعذر التحقق. في الواجهة تُعطّل الأزرار نفسها ويشرح عنوانها ضرورة تحديث المكتبة، بينما يبقى زر التحديث متاحاً كمسار تعافٍ.

إنشاء التيم يتطلب حساباً محفوظاً، queue للحساب، rate limiter، فحص جاهزية، وتجهيز Portal، ثم يعاد تحميل التيم للتأكد من ظهوره قبل إبلاغ النجاح. نقل التطبيق إلى تيم يتطلب تأكيد ملكية التيم والتطبيق، سعة أقل من 25 تطبيقاً، MFA مؤكد عند الحاجة، ثم تأكيد `team_id` بعد النقل. Reset All يتحقق من أن كل بوت مطلوب موجود في المكتبة الكاملة، ولا يخفي أخطاء Portal أو MFA أو المحاكاة؛ وتُرسل تفاصيل فشل كل بوت عبر SSE مع `appId` و`name` و`message` و`code`.

## الواجهة والسجل

تم توحيد نتائج Live Log عبر metadata للمرحلة والتأكيد والنتيجة الجزئية، مع رسائل قصيرة تميز `تم التحقق` عن `غير مكتمل`. نافذة Reset All تعرض الأخطاء والمتبقي وتبقى مفتوحة عند الفشل أو الإلغاء. أزيلت الرموز التعبيرية والأسهم من النصوص التنفيذية المتبقية واستُبدلت بأيقونات SVG أو عبارات نصية، مع دعم الألوان الفاتحة والداكنة والمقاسات الصغيرة.

## الاختبارات والحدود

نجحت فحوصات صياغة Node و`git diff --check` وSmoke المحلي الموسع واختبار rate-limit المنفصل. الاختبارات تغطي أخطاء الإدخال، المكتبة بلا حساب، حساباً غير معروف، التيمات بلا حساب، بدء جلسة غير صالح، Reset All غير صالح، الإيقاف أثناء الخمول، وتحليل route/shared/global والـ ambiguous 429. لم تُنفذ عمليات Discord الحية أو MFA أو CAPTCHA لعدم توفر حسابات مصرح بها لهذا الغرض.

| الفحص | النتيجة |
|---|---|
| `npm run test:operations` | ناجح |
| `npm run test:smoke` | ناجح |
| `node --check` للملفات المعدلة | ناجح |
| `git diff --check` | ناجح |
| معاينة المتصفح المحلي وConsole | لا أخطاء Console؛ أزرار bulk معطلة بلا حساب |
| Discord/MFA/CAPTCHA حي | لم يُنفذ |

## المراجع

[1]: https://docs.discord.com/developers/topics/rate-limits "Discord Developer Documentation — Rate Limits"
[2]: https://support-dev.discord.com/hc/en-us/articles/6223003921559-My-Bot-is-Being-Rate-Limited "Discord Developer Support — My Bot is Being Rate Limited"
